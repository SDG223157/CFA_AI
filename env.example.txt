# Optional: root folder to search (defaults to this repo folder)
CFA_AI_ROOT=/path/to/your/data

# Recommended: OpenRouter (OpenAI-compatible)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=qwen/qwen2.5-vl-7b-instruct
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional (recommended by OpenRouter):
# OPENROUTER_HTTP_REFERER=https://your-domain.example
# OPENROUTER_X_TITLE=Daily Tasks Dashboard

# Option A: OpenAI (requires network access)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Option B: Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1

# Optional: force-disable ollama fallback
DISABLE_OLLAMA=false

